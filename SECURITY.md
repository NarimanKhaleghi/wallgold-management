# Security Policy — سیاست امنیتی

## نگاه کلی / Overview

WallGold Manager با رویکرد «امنیت در عمق» (Defense in Depth) طراحی شده است. این سند مدل تهدید و لایه‌های دفاعی پیاده‌سازی‌شده را شرح می‌دهد.

## لایه‌های دفاعی پیاده‌شده

| لایه | مکانیزم |
|---|---|
| احراز هویت | رمز عبور با PBKDF2-SHA256 (۱۰۰٬۰۰۰ تکرار، نمک ۱۶ بایتی اختصاصی) + TOTP مطابق RFC 6238 (HMAC-SHA1، ۶ رقم، ±۱ گام) + کدهای پشتیبان یک‌بارمصرف (فقط هش SHA-256 ذخیره می‌شود) |
| نشست‌ها | توکن ۲۵۶ بیتی تصادفی، ذخیره **هش‌شده** در دیتابیس، کوکی `HttpOnly` + `SameSite=Strict` + `Secure`، انقضای مطلق ۱ ساعته، ابطال نشست‌های دیگر با تغییر رمز |
| Brute Force | محدودسازی نرخ مبتنی بر IP در D1: ۵ ورود ناموفق/۱۰ دقیقه → قفل ۱۵ دقیقه؛ ۱۰ کد 2FA ناموفق/۵ دقیقه → قفل |
| رمزنگاری در سکون | توکن‌های API وال‌گلد با AES-256-GCM (IV تصادفی ۱۲ بایتی + AuthTag) رمزنگاری می‌شوند؛ کلید از Secret محیطی یا تولید خودکار در D1 |
| CSRF | کوکی SameSite=Strict + هدر الزامی `X-Requested-With: fetch` + بررسی تطابق Origin روی همه متدهای تغییردهنده |
| XSS | CSP سختگیرانه: `script-src 'self'` (بدون اسکریپت درون‌خطی)، `object-src 'none'`؛ فریم‌ورک React با escape پیش‌فرض |
| Clickjacking | `X-Frame-Options: DENY` + `frame-ancestors 'none'` |
| نشت از کش | `Cache-Control: no-store` روی تمام پاسخ‌های API |
| اعتبارسنجی | بررسی سمت سرور همه ورودی‌ها (مقدار تا ۳ اعشار، محدودیت‌های بازار، موجودی) + سقف ۱۰۰KB بدنه درخواست |
| جداسازی توکن | توکن‌های وال‌گلد فقط سمت Worker استفاده می‌شوند؛ هرگز در پاسخ API، لاگ یا کوکی ظاهر نمی‌شوند |
| خروج خروجی | تنها میزبان خروجی مجاز: `api.wallgold.ir` |

## مدل تهدید

- **مهاجم شبکه (MITM):** همه ترافیک روی HTTPS ورکر کلودفلر است؛ کوکی‌ها Secure و SameSite=Strict هستند.
- **مهاجم با دسترسی مرورگر (XSS):** CSP بدون اسکریپت درون‌خطی + عدم وجود توکن وال‌گلد در مرورگر (فقط کوکی نشست HttpOnly).
- **مهاجم با دسترسی به دیتابیس D1:** فقط هش رمز، هش نشست، توکن‌های AES-GCM رمزنگاری‌شده و هش کدهای پشتیبان موجود است.
- **Brute Force آنلاین:** محدودسازی نرخ + هزینه محاسباتی PBKDF2 + فضای ۲FA.
- **درخواست متقاطع (CSRF):** سه لایه دفاع مستقل.

## گزارش آسیب‌پذیری

آسیب‌پذیری‌ها را به‌صورت خصوصی از طریق [بخش Security ریپازیتوری GitHub](https://github.com/NarimanKhaleghi/wallgold-management/security/advisories) گزارش کنید. لطفاً ۹۰ روز فرصت برای رفع بدهید.

## نکات عملیاتی

1. در پروداکشن، `ENCRYPTION_KEY` را به‌عنوان Secret تنظیم کنید (قبل از افزودن حساب‌ها).
2. 2FA را بلافاصله پس از راه‌اندازی فعال کنید و کدهای پشتیبان را آفلاین ذخیره کنید.
3. رمز قوی (۱۲+ کاراکتر) انتخاب کنید.
4. اگر رمز یا نشست مشکوک است: تغییر رمز (ابطال همه نشست‌های دیگر) + بررسی نشست‌های فعال در تنظیمات.
5. دسترسی به داشبورد Cloudflare خود را با 2FA حساب Cloudflare محافظت کنید.

## بازیابی قفل‌شدن (Lockout Recovery)

اگر هم رمز و هم 2FA از دست رفته باشد:

```bash
# با wrangler (متصل به حساب خودتان)
npx wrangler d1 execute wallgold-management --remote --command \
  "DELETE FROM app_settings WHERE key IN ('auth.password_hash','auth.totp_secret','auth.totp_pending','auth.totp_enabled')"
```

سپس صفحه را رفرش کنید — ویزارد راه‌اندازی اولیه دوباره اجرا می‌شود (حساب‌های افزوده‌شده حفظ می‌شوند).
